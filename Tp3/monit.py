import argparse
import psutil
import datetime


# parser = argparse.ArgumentParser(description="Usage: Monitor the system")
# parser.add_argument("check", help="Check resource and register this data in a file")
# parser.add_argument("list", action="store", help="List the latest reports")
# parser.add_argument("avg", action="store", type=int, const=1, nargs="?", help="Calculate the last X values of the last hours")
# parser.add_argument("get", action="store", help="get + [OPTION]")
# parser.add_argument("last", action="store", help="The last report")
# args = parser.parse_args()


# if args.get and (args.avg is None and args.last is None):
#   print("salut")
